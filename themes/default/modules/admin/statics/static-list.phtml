<div class="row">
	<div class="column">
		
		<form class="ui form panel-news" method="post" action="<?= ADMIN_URL; ?>&do=statics&op=delete">
			
			<div class="ui fluid action input">
				<input class="adm-search-input" id="adm-search-id" placeholder="<?= $this->l10n->gettext('search_placeholder'); ?>" type="text">
				<button class="ui basic button adm-search" data-for="#adm-search-id"><?= $this->l10n->gettext('search'); ?></button>
			</div>
			
			<table class="ui celled table">
				<thead>
					<tr>
						<th class="collapsing">
							<a href="#" class="sort-trigger" data-field="id" data-order="desc">#</a>
						</th>
						<th>
							<a href="#" class="sort-trigger" data-field="title" data-order="desc"><?= $this->l10n->gettext('stc_field_name'); ?></a>
						</th>
						<th>
							<a href="#" class="sort-trigger" data-field="perm" data-order="desc"><?= $this->l10n->gettext('stc_field_perm'); ?></a>
						</th>
						<th class="right aligned collapsing action">
							<div class="ui checkbox">
								<input type="checkbox" class="check-all" data-for="check-input">
								<label></label>
							</div>
						</th>
					</tr>
				</thead>
				
				<tbody>
					<?= $data['STATICS']; ?>
				</tbody>
			</table>
			
			<div class="field">
				<div id="actions" class="ui fluid buttons">
					<a class="ui primary button" href="<?= ADMIN_URL; ?>&do=statics&op=add">
						<i class="icon plus"></i> <?= $this->l10n->gettext('add'); ?>
					</a>
					<a class="ui primary button edit" data-for="check-input" data-link="<?= ADMIN_URL; ?>&do=statics&op=edit&id=">
						<i class="icon pencil"></i> <?= $this->l10n->gettext('edit'); ?>
					</a>
					<button class="ui negative button remove" data-for="check-input" name="delete" type="submit">
						<i class="icon times"></i> <?= $this->l10n->gettext('delete'); ?>
					</button>
				</div>
			</div>
			
			<? if (!empty($data['PAGINATION'])) {?>
				<div class="ui one column stackable center aligned page grid">
					<div class="column wide">
						<?= $data['PAGINATION']; ?>
					</div>
				</div>
			<? } ?>
			
		</form>
		
	</div>
</div>
