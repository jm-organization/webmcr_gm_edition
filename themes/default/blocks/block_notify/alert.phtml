<?php foreach ($messages as $message) {
    ?>

    <div class="ui black message <?= $message['type'] ?>">
        <i class="close icon"></i>
        <div class="header"><?= $message['title'] ?></div>
        <p><?= $message['text'] ?></p>
    </div>

    <?php
}
